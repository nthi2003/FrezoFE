# ============================================================
# Landing page environment
# Copy sang `.env` (KHÔNG commit) hoặc `.env.local` cho dev.
# ============================================================

# Base URL của BE. Trong prod nếu landing + BE cùng domain thì để trống → fetch same-origin.
# Dev local: chạy BE ở http://localhost:7410/api (context-path /api) → VITE_API_BASE=http://localhost:7410
VITE_API_BASE=http://localhost:7410
